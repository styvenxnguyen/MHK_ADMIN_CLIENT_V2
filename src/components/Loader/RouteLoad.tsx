import React from 'react'
import Progress from './Progress'

const RouteLoader = () => {
  return (
    <React.Fragment>
      <Progress isAnimating />
    </React.Fragment>
  )
}

export default RouteLoader
